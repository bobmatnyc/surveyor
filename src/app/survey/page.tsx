import { SurveySelection } from '@/components/survey/survey-selection';

export default function SurveyPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
      <SurveySelection />
    </div>
  );
}